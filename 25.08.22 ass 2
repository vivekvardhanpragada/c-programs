#include<stdio.h>
#include<ctype.h>
int main()
{
	char str[50];
	int i;
	printf("enter the string:\n");
	gets(str);
	for(i=0;str[i]!='\0';i++)
	{
		if((i%2)==0)
		str[i]=tolower(str[i]);
		else
		str[i]=toupper(str[i]);
	}
	printf("%s\n",str);
	return 0;
}
