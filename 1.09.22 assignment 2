#include<stdio.h>
int factorial(int n)
{
   	if(n==0){
   		return(1);
	    }
	   if(n<0){
	   	return(0);
	    }
	   if(n>0){
	   	return(n*factorial(n-1));
	    }
}
main(){
	int n,f;
	printf("enter the number:");
	scanf("%d",&n);
	f=factorial(n);
	printf("%d",f);
}
